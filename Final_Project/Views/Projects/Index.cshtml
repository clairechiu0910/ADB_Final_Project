<div class="wrapper">
    <div class="profile-background">
        <div class="filter-black"></div>
    </div>
    <div id="app_projects">
        <div id="my-tab-content" class="tab-content">
            <div class="tab-pane active" id="follows">
                <div class="row">
                    <div class="col-md-6 col-md-offset-3">
                        <ul class="list-unstyled follows">
                            <template v-for="item in projects">
                                <li>
                                    <div class="row">
                                        <h3>{{item.title}}</h3>
                                        <div class="col-md-7 col-xs-4">
                                            <span class="label label-default">{{item.project_type}}</span>
                                            <small>{{item.description}}</small>
                                        </div>
                                        <div class="col-md-3 col-xs-2">
                                            <div class="text-right text">
                                                <button href="#fakelink" class="btn btn-xs">Join Project</button>
                                            </div>
                                        </div>
                                    </div>
                                </li>
                                <hr />
                            </template>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    fetch('@Url.Action("GetProjects", "Projects")')
        .then(function (response) {
            response.json().then(function (data) {
                app_projects._data.projects = data;
                console.log(app_projects._data.projects);
            });
        });
    var app_projects = new Vue({
        el: '#app_projects',
        data: {
            projects: null
        }
    });
</script>
